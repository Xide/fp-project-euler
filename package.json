{
  "name": "fp-project-euler",
  "version": "1.0.0",
  "description": "Functional programming solutions to the project Euler",
  "scripts": {
    "build": "./node_modules/.bin/babel -d dist src/",
    "test": "npm run build && ./node_modules/.bin/mocha dist/*spec.js",
    "validate": "npm run build && ./node_modules/.bin/mocha dist/answers_check.js"
  },
  "author": "Germain GAU",
  "license": "MIT",
  "dependencies": {
    "ramda": "^0.25.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "chai": "^4.1.2",
    "mocha": "^4.0.1"
  }
}
